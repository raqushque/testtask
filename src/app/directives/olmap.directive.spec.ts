import { OLMapDirective } from './olmap.directive';

describe('OLMapDirective', () => {
  it('should create an instance', () => {
    const directive = new OLMapDirective();
    expect(directive).toBeTruthy();
  });
});
